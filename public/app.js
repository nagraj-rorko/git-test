const ports = [
  { city: 'Rotterdam', country: 'Netherlands', code: 'NLRTM', tag: 'Gateway to the North Sea', stat: '14.8M TEU', detail: 'Deep-water access, multimodal rail and Europe’s most connected logistics corridor.' },
  { city: 'Antwerp', country: 'Belgium', code: 'BEANR', tag: 'The industrial heart', stat: '13.5M TEU', detail: 'A dense network of terminals, inland waterways and specialist cargo capabilities.' },
  { city: 'Hamburg', country: 'Germany', code: 'DEHAM', tag: 'Where trade meets precision', stat: '7.7M TEU', detail: 'Reliable northern access supported by smart yard operations and resilient rail links.' },
  { city: 'Valencia', country: 'Spain', code: 'ESVLC', tag: 'The Mediterranean pulse', stat: '5.5M TEU', detail: 'Strategic Mediterranean positioning for faster routes into Southern Europe and beyond.' }
];

const nav = (href, label) => `<a class="text-link" href="${href}">${label} <span>↗</span></a>`;
const page = (content) => `<div class="page-wrap">${content}</div>`;

function home() { return page(`<section class="hero" id="home"><div class="hero-copy"><p class="eyebrow">EUROPEAN PORT INTELLIGENCE <span></span></p><h1>The port is<br /><em>the pulse</em> of trade.</h1><p class="hero-lede">We connect cargo, infrastructure and ambition across Europe’s most vital gateways.</p><div class="hero-actions"><a class="button button-gold" href="#ports">Explore our ports <span>↗</span></a>${nav('#approach', 'How we work')}</div></div><div class="hero-meta"><span>52° 00' N</span><span class="meta-line"></span><span>4° 08' E</span></div><div class="hero-scroll">SCROLL TO DISCOVER <span>↓</span></div></section><section class="intro-section"><div><p class="eyebrow">01 / THE NETWORK</p><h2>Moving Europe<br /><em>with intent.</em></h2></div><div class="intro-text"><p>From first berth to final mile, Harborline makes port operations clearer, faster and ready for what’s next.</p>${nav('#services', 'See our capabilities')}</div></section><section class="stat-strip"><div><strong>04</strong><span>strategic<br />gateways</span></div><div><strong>98.4%</strong><span>operational<br />reliability</span></div><div><strong>24/7</strong><span>live port<br />intelligence</span></div></section>`); }

function portsPage() { return page(`<section class="inner-hero"><p class="eyebrow">02 / OUR PORTS</p><h1>Four gateways.<br /><em>One clear view.</em></h1><p>Strategic access to the infrastructure that keeps Europe moving.</p></section><section class="ports-grid">${ports.map((p, i) => `<article class="port-card"><div class="port-number">0${i+1}</div><div class="port-code">${p.code}</div><h3>${p.city}</h3><p class="port-country">${p.country} · ${p.tag}</p><div class="port-foot"><span>${p.stat}</span><span>Explore port ↗</span></div><p class="port-detail">${p.detail}</p></article>`).join('')}</section>`); }

function servicesPage() { return page(`<section class="inner-hero"><p class="eyebrow">03 / SERVICES</p><h1>Make complexity<br /><em>work for you.</em></h1><p>A practical layer of intelligence for every stage of the port journey.</p></section><section class="service-list"><article><span>01</span><div><h3>Port Operations</h3><p>Berth planning, yard flow and terminal coordination that reduce friction at the water’s edge.</p></div><b>↗</b></article><article><span>02</span><div><h3>Trade Advisory</h3><p>Market context and route intelligence for decisions that stay useful beyond today’s forecast.</p></div><b>↗</b></article><article><span>03</span><div><h3>Digital Visibility</h3><p>One live operating picture across cargo milestones, exceptions and the people who resolve them.</p></div><b>↗</b></article><article><span>04</span><div><h3>Sustainable Transition</h3><p>Actionable pathways to lower emissions, optimize energy and build resilient infrastructure.</p></div><b>↗</b></article></section>`); }

function approachPage() { return page(`<section class="inner-hero"><p class="eyebrow">04 / THE APPROACH</p><h1>Clarity in the<br /><em>current.</em></h1><p>We combine local knowledge with a wider view, so every decision has somewhere to go.</p></section><section class="approach-layout"><div class="orbit"><div class="orbit-core">LIVE<br /><span>PORT<br />VIEW</span></div><i class="orb-dot dot-one"></i><i class="orb-dot dot-two"></i><i class="orb-dot dot-three"></i></div><div class="principles"><div><span>01</span><h3>See the whole system.</h3><p>We map the connections between a berth, a rail line, a regulation and a customer promise.</p></div><div><span>02</span><h3>Act on what matters.</h3><p>Clear priorities turn live information into momentum—not another dashboard to manage.</p></div><div><span>03</span><h3>Leave a stronger wake.</h3><p>Every project should improve the port’s resilience, efficiency and long-term value.</p></div></div></section>`); }

function contactPage() { return page(`<section class="contact-layout"><div><p class="eyebrow">05 / CONTACT</p><h1>Let’s move<br /><em>forward.</em></h1><p class="contact-copy">Tell us where the current is taking you. We’ll bring the right people and perspective to the table.</p><div class="contact-details"><span>AMSTERDAM · ROTTERDAM · HAMBURG</span><a href="mailto:hello@harborline.eu">hello@harborline.eu</a></div></div><form id="contact-form"><label>Name<input name="name" required placeholder="Your name" /></label><label>Work email<input name="email" type="email" required placeholder="you@company.com" /></label><label>How can we help?<textarea name="message" rows="4" placeholder="A little context goes a long way."></textarea></label><button class="button button-gold" type="submit">Send enquiry <span>↗</span></button><p class="form-status" role="status"></p></form></section>`); }

const routes = { '#home': home, '#ports': portsPage, '#services': servicesPage, '#approach': approachPage, '#contact': contactPage };
function bindTilt() { if (matchMedia('(prefers-reduced-motion: reduce)').matches) return; document.querySelectorAll('.port-card, .service-list article').forEach(card => { card.addEventListener('pointermove', e => { const box = card.getBoundingClientRect(); const x = (e.clientX - box.left) / box.width - .5; const y = (e.clientY - box.top) / box.height - .5; card.style.setProperty('--rx', `${y * -5}deg`); card.style.setProperty('--ry', `${x * 5}deg`); }); card.addEventListener('pointerleave', () => { card.style.setProperty('--rx', '0deg'); card.style.setProperty('--ry', '0deg'); }); }); const hero = document.querySelector('.hero'); if (hero) hero.addEventListener('pointermove', e => { hero.style.setProperty('--px', `${(e.clientX / innerWidth - .5) * -10}px`); hero.style.setProperty('--py', `${(e.clientY / innerHeight - .5) * -6}px`); }); }
function render() { const route = location.hash || '#home'; document.querySelector('#app').innerHTML = (routes[route] || home)(); document.querySelectorAll('.main-nav a').forEach(a => a.classList.toggle('active', a.getAttribute('href') === route)); window.scrollTo({ top: 0, behavior: 'instant' }); bindForm(); bindTilt(); }
function bindForm() { const form = document.querySelector('#contact-form'); if (!form) return; form.addEventListener('submit', async e => { e.preventDefault(); const status = form.querySelector('.form-status'); status.textContent = 'Sending…'; const data = Object.fromEntries(new FormData(form)); const res = await fetch('/api/contact', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(data) }); const result = await res.json(); status.textContent = result.message; if (result.ok) form.reset(); }); }
window.addEventListener('hashchange', render); render();
